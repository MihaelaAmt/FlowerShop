@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Presentation of flowers</h2>
<h4>Our selection - just for you!</h4>

<div class="row">
    @foreach (var flower in Model.FlowerPresentation)
    {
        @Html.Partial("FlowerPresentationSummary", flower)
    }
</div>
