@using System.Net
@using System.Threading.Tasks
@model FlowerDetailViewModel

<div class="thumbnail">
    <img alt="@Model.Flower.Name" src="@Model.Flower.ImageUrl">
    <div class="caption-full">
        <h3 class="pull-right">@Model.Flower.Price</h3>
        <h3>
            <a href="#">@Model.Flower.Name</a>
        </h3>
        <h4>@Model.Flower.ShortDescription</h4>
        <p>@Model.Flower.LongDescription</p>
        <p class="button">
            <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="AddToShoppingCart"
               asp-route-pieId="@Model.Flower.FlowerId">Add to cart</a>
        </p>
    </div>
</div>
